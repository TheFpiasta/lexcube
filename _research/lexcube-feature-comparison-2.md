# Lexcube Feature Comparison (Jupyter vs Web)

This table lists user-facing capabilities (what you can do), with environment-specific access notes and code entry points.

| Feature                         | Description                                                                               | Jupyter | Web |
|---------------------------------|-------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Visualize 3D data cube          | Render a 3D volume from a 3D array and display it interactively.                          | **ACCESS**: `Cube3DWidget(data)` or `.plot()`<br>**ENTRY**: `lexcube/cube3d.py` + widget bridge `src/widget.ts`<br>**RENDER**: `src/lexcube-client/src/client/rendering.ts`<br>**HOW TO USE**: pass a 3D `numpy.ndarray` or `xarray.DataArray` into `Cube3DWidget` in a notebook to render the cube | **ACCESS**: standalone client page UI<br>**ENTRY/UI**: `src/lexcube-client/src/client/index.html` + `src/lexcube-client/src/client/interaction.ts`<br>**RENDER**: `src/lexcube-client/src/client/rendering.ts`<br>**HOW TO USE**: open the web client and use the Cube/Parameter selects to render a cube |
| Navigate camera                 | Rotate, pan, and zoom the scene to view the cube from different angles.                   | **ACCESS**: mouse/touch on canvas<br>controls in `src/lexcube-client/src/client/OrbitControls.ts` and `src/lexcube-client/src/client/interaction.ts`<br>same in widget mode<br>one-finger rotate, two-finger pinch to zoom, three-finger pan on touch<br>**HOW TO USE**: left-drag to orbit, mouse wheel or middle-drag to zoom, right-drag to pan | **ACCESS**: mouse/touch on canvas<br>controls in `src/lexcube-client/src/client/OrbitControls.ts` and `src/lexcube-client/src/client/interaction.ts`<br>one-finger rotate, two-finger pinch to zoom, three-finger pan on touch<br>**HOW TO USE**: left-drag to orbit, mouse wheel or middle-drag to zoom, right-drag to pan |
| Select subranges (X/Y/Z)        | Choose a subset of the cube by adjusting range sliders for each axis.                     | **ACCESS**: in-widget sliders from `src/widget.ts` template + optional external sliders via `Cube3DWidget.show_sliders()` in `lexcube/cube3d.py`<br>programmatic control via `xlim/ylim/zlim` traitlets in `lexcube/cube3d.py`<br>selection logic in `src/lexcube-client/src/client/interaction.ts`<br>you can also call `show_sliders()` to display external ipywidgets sliders linked to the limits<br>**HOW TO USE**: drag the X/Y/Z slider handles or set `xlim/ylim/zlim` in Python | **ACCESS**: sliders in `src/lexcube-client/src/client/index.html` and `src/lexcube-client/src/client/interaction.ts`<br>updates URL fragment when not in widget mode<br>open the Options panel (data-select button) to reveal the Current Selection sliders<br>**HOW TO USE**: drag the X/Y/Z slider handles to set the range |
| Zoom into faces                 | Zoom in/out on cube faces to change visible window size.                                  | **ACCESS**: mouse wheel / touch gestures<br>selection resize in `src/lexcube-client/src/client/interaction.ts` `changeZoomOnFace`<br>**HOW TO USE**: scroll or pinch over a face to zoom in/out use the mouse wheel while the cursor is over a cube face (not the background), or two-finger pinch on a face to zoom the selection window | **ACCESS**: mouse wheel / touch gestures<br>selection resize in `src/lexcube-client/src/client/interaction.ts` `changeZoomOnFace`<br>**HOW TO USE**: scroll or pinch over a face to zoom in/out use the mouse wheel while the cursor is over a cube face (not the background), or two-finger pinch on a face to zoom the selection window |
| Animate along a dimension       | Play an animation by stepping a chosen axis through a range.                              | **ACCESS**: animation UI in widget template `src/widget.ts`<br>logic in `src/lexcube-client/src/client/interaction.ts`<br>**HOW TO USE**: select the animation dimension and range in the animation panel, then press play use the play/stop buttons, set Increment per Step, Visible Window, and Animation Speed sliders, and optionally toggle “Only animate last selection (X to Y)” | **ACCESS**: animation UI in `src/lexcube-client/src/client/index.html`<br>logic in `src/lexcube-client/src/client/interaction.ts`<br>**HOW TO USE**: select the animation dimension and range in the animation panel, then press play use the play/stop buttons, set Increment per Step, Visible Window, and Animation Speed sliders, and optionally toggle “Only animate last selection (X to Y)” |
| Record animation (GIF/MP4/WebM) | Capture animation frames and export as GIF/MP4/WebM.                                      | **ACCESS**: animation recording checkbox + format in widget UI<br>implementation in `src/lexcube-client/src/client/rendering.ts` and `src/lexcube-client/src/client/interaction.ts`<br>in the animation dropdown, check "Record Animation" to reveal the Start Recording button and MP4/WebM/GIF format selector, then click Start Recording and Stop Recording to finalize the download<br>**HOW TO USE**: enable recording, choose a format, play the animation, then download the output | **ACCESS**: animation recording checkbox + format in `src/lexcube-client/src/client/index.html`<br>implementation in `src/lexcube-client/src/client/rendering.ts`<br>in the animation dropdown, check "Record Animation" to reveal the Start Recording button and MP4/WebM/GIF format selector, then click Start Recording and Stop Recording to finalize the download<br>**HOW TO USE**: enable recording, choose a format, play the animation, then download the output |
| Export screenshot (PNG)         | Download a still image of the cube (optionally with UI overlays).                         | **ACCESS**: `Cube3DWidget.savefig(include_ui, dpi_scale)` in `lexcube/cube3d.py`<br>message handled in `src/widget.ts` -> `downloadScreenshotFromUi` in `src/lexcube-client/src/client/rendering.ts`<br>**HOW TO USE**: call `savefig()` in Python pass `include_ui` and `dpi_scale` to `savefig` to control UI overlays and output resolution | **ACCESS**: download button in `src/lexcube-client/src/client/index.html`<br>triggers `downloadScreenshotFromUi(true)` in `src/lexcube-client/src/client/interaction.ts`<br>the button is shown in studio mode (non-widget) and triggers the PNG download<br>**HOW TO USE**: click the download image button |
| Export print template           | Generate a foldable paper cube template (SVG/PNG).                                        | **ACCESS**: `Cube3DWidget.save_print_template()` in `lexcube/cube3d.py`<br>widget handles `download_print_template_request` in `src/widget.ts` and calls `rendering.startDownloadPrintTemplate()`<br>UI result panel (Download SVG/PNG + Add custom note) is in the widget template `src/widget.ts`<br>**HOW TO USE**: call `save_print_template()` in Python, then download SVG/PNG from the print-template result panel | **ACCESS**: UI button in `src/lexcube-client/src/client/index.html`<br>render/export in `src/lexcube-client/src/client/rendering.ts`<br>includes download links for SVG/PNG and note edit in UI<br>**HOW TO USE**: click the print template button, then download SVG/PNG from the print-template result panel (Download SVG/PNG links and Add custom note) |
| Add custom note to template     | Insert a user-defined note into the print template before downloading.                    | **ACCESS**: “Add custom note” link in widget template<br>prompt + SVG injection in `src/lexcube-client/src/client/interaction.ts` around print template logic<br>UI element in `src/widget.ts`<br>**HOW TO USE**: click “Add custom note” and enter text in the prompt after submitting, the note section is revealed in the template and the link label changes to “Edit your custom note” for subsequent edits | **ACCESS**: “Add custom note” link in `src/lexcube-client/src/client/index.html`<br>prompt + SVG injection in `src/lexcube-client/src/client/interaction.ts` `download-print-template-result-edit-note`<br>**HOW TO USE**: click “Add custom note” and enter text in the prompt after submitting, the note section is revealed in the template and the link label changes to “Edit your custom note” for subsequent edits |
| Choose colormap                 | Switch between built-in colormaps.                                                        | **ACCESS**: colormap UI in widget template `src/widget.ts`<br>also Python `cmap` traitlet in `lexcube/cube3d.py`<br>selection logic in `src/lexcube-client/src/client/interaction.ts`<br>you can pass `cmap` into `Cube3DWidget(...)` or set `widget.cmap = "viridis"` to update programmatically<br>**HOW TO USE**: open the colormap list and click a colormap (or set `cmap` in Python) expand a colormap category header (►/▼) and click a colormap button | **ACCESS**: colormap UI in `src/lexcube-client/src/client/index.html`<br>selection logic in `src/lexcube-client/src/client/interaction.ts`<br>**HOW TO USE**: open the colormap list and click a colormap expand a category header (►/▼) and click a colormap button in the list |
| Set colormap bounds             | Adjust min/max range, flip colormap, and toggle statistical auto-bounds.                  | **ACCESS**: UI controls + Python `vmin`/`vmax` traitlets<br>logic in `src/lexcube-client/src/client/interaction.ts` + `src/lexcube-client/src/client/tiledata.ts`<br>widget sync in `src/widget.ts`<br>in Python pass `vmin`/`vmax` to `Cube3DWidget(...)`<br>**HOW TO USE**: edit the min/max inputs or set `vmin`/`vmax` in Python, and use flip/auto controls in the UI after entering min/max values, submit the Colormap Range form (press Enter) to apply, toggle “Flip Colormap,” and in expert mode toggle the “For automatic boundaries...” checkbox | **ACCESS**: UI controls in `src/lexcube-client/src/client/index.html`<br>logic in `src/lexcube-client/src/client/interaction.ts` + `src/lexcube-client/src/client/tiledata.ts`<br>**HOW TO USE**: edit the min/max inputs and use flip/auto controls enter min/max in the Colormap Range fields, press Enter to apply, and toggle “Flip Colormap” plus the “For automatic boundaries...” checkbox in expert mode |
| Use custom colormap data        | Provide an explicit colormap array instead of selecting by name.                          | **ACCESS**: pass list to `cmap` in `lexcube/cube3d.py`<br>conversion + selection in `src/widget.ts` `colormapChanged` and `src/lexcube-client/src/client/interaction.ts` `selectColormapByData`<br>**HOW TO USE**: set `cmap` to a list of RGB triples in Python the widget converts the list via `convertColormapDataToRGB8` and applies it with `selectColormapByData` | |
| Per-parameter colormap defaults | Apply parameter-specific colormap presets and bounds from metadata.                       | **ACCESS**: metadata lookup in `src/lexcube-client/src/client/interaction.ts` using `src/lexcube-client/src/content/parameterCustomColormaps.json`<br>in widget mode, `fixedColormap` + `fixedColormapFlipped` are applied on selection, but fixed min/max are not auto-applied (use `vmin`/`vmax` traitlets if needed) | **ACCESS**: metadata lookup in `src/lexcube-client/src/client/interaction.ts`<br>data in `src/lexcube-client/src/content/parameterCustomColormaps.json`<br>**HOW TO USE**: select a parameter and the defaults apply automatically add the parameter key to `parameterCustomColormaps.json` with colormapMinimumValue/colormapMaximumValue/colormap/colormapFlipped so selection applies those presets |
| Color scale legend              | Show color scale with numeric labels and units.                                           | **ACCESS**: UI elements in `src/widget.ts` template<br>updates in `src/lexcube-client/src/client/interaction.ts`<br>it updates as you change colormap or bounds<br>**HOW TO USE**: read the scale under the colormap use the bottom-left color-scale bar with three numeric labels and the unit label beneath the colormap options | **ACCESS**: UI elements in `src/lexcube-client/src/client/index.html`<br>updates in `src/lexcube-client/src/client/interaction.ts`<br>it updates as you change colormap or bounds<br>**HOW TO USE**: read the scale under the colormap use the bottom-left color-scale bar with three numeric labels and the unit label beneath the colormap options |
| Hover inspect values            | Show data values and coordinates under the cursor.                                        | **ACCESS**: hover on cube<br>computed in `src/lexcube-client/src/client/interaction.ts` `updateHoverInfo`<br>hover info is shown in the bottom-left `hover-info-ui` panel and only appears while the cursor is over the cube (not the background)<br>**HOW TO USE**: move the cursor over a face to see values and coordinates | **ACCESS**: hover on cube<br>computed in `src/lexcube-client/src/client/interaction.ts` `updateHoverInfo`<br>hover info is shown in the bottom-left `hover-info-ui` panel and only appears while the cursor is over the cube (not the background)<br>**HOW TO USE**: move the cursor over a face to see values and coordinates |
| Axis labels and bounds          | Display axis names and min/max labels around the cube.                                    | **ACCESS**: overlay elements in `src/widget.ts` template<br>updated in `src/lexcube-client/src/client/interaction.ts`<br>**HOW TO USE**: read the axis labels around the cube and adjust selection to update them rotate/orbit the view so labels reposition and remain visible as the camera changes | **ACCESS**: overlay elements in `src/lexcube-client/src/client/index.html`<br>updated in `src/lexcube-client/src/client/interaction.ts`<br>**HOW TO USE**: read the axis labels around the cube and adjust selection to update them rotate/orbit the view so labels reposition and remain visible as the camera changes |
| GeoJSON region borders          | Overlay region borders on the cube.                                                       | **ACCESS**: `Cube3DWidget.overlay_geojson()` or `overlaid_geojson` traitlet in `lexcube/cube3d.py`<br>validation in `lexcube/cube3d.py`<br>requires xarray input<br>render in `src/lexcube-client/src/client/rendering.ts` `loadRegionBordersFromGeoJsonForWidget`<br>**HOW TO USE**: call `overlay_geojson(...)` or set `overlaid_geojson` in Python (set empty to clear) pass a GeoJSON dict/JSON string/URL/file path (and optional `color`) to `overlay_geojson`, or set `overlaid_geojson=''` to remove borders | **ACCESS**: auto-load Natural Earth borders when geospatial context is available<br>`src/lexcube-client/src/client/rendering.ts` `loadRegionBordersFromNaturalEarth` + `geojson-loader.worker.ts`<br>no UI for custom GeoJSON<br>**HOW TO USE**: load a geospatial dataset so borders appear automatically in non-widget mode the client fetches `/ne_<resolution>m_admin_0_countries.geojson` automatically, so open a geospatial dataset and let it auto-load (custom GeoJSON not supported) |
| Show current GPS location       | Display browser geolocation on the cube.                                                  | | **ACCESS**: GPS button in src/lexcube-client/src/client/index.html<br>geolocation via navigator.geolocation in src/lexcube-client/src/client/interaction.ts<br>click the GPS button again to stop tracking<br>GPS only starts when geospatial context is available (otherwise an alert is shown)<br>**HOW TO USE**: click the GPS button and grant location permission |
| Fullscreen view                 | Toggle fullscreen mode.                                                                   | **ACCESS**: fullscreen button in widget template<br>logic in `src/lexcube-client/src/client/interaction.ts`<br>**HOW TO USE**: click the fullscreen button to enter or exit fullscreen use the toolbar `fullscreen-button` in `src/widget.ts` which toggles `requestFullscreen`/`exitFullscreen` on the widget container via the click handler | **ACCESS**: fullscreen button in `src/lexcube-client/src/client/index.html`<br>logic in `src/lexcube-client/src/client/interaction.ts`<br>**HOW TO USE**: click the fullscreen button to enter or exit fullscreen click the toolbar `fullscreen-button` (title 'Enter/exit fullscreen') which triggers `requestFullscreen`/`exitFullscreen` in `interaction.ts` |
| Dataset and parameter info      | View dataset metadata and parameter attribution in the UI.                                | **ACCESS**: dataset info dialog in widget template<br>data provided by lexcube/lexcube_server/src/lexcube_widget.py (api_metadata from xarray attrs/coords)<br>attribution parsing in src/lexcube-client/src/client/interaction.ts<br>**HOW TO USE**: click the corner logo in the widget (dataset-info-corner list) to open the dialog, then click outside or Acknowledge to close | **ACCESS**: dataset info dialog in src/lexcube-client/src/client/index.html<br>metadata from /api/datasets and /api/datasets/{id}<br>attribution parsing in src/lexcube-client/src/client/interaction.ts with parameterMetadataAttribution.json<br>**HOW TO USE**: click the dataset info corner list (e.g., the underlined “Data attribution and license” entry) to open the dialog, then click outside or Acknowledge to close |
| Switch datasets and parameters  | Choose among multiple datasets and parameters.                                            | | **ACCESS**: dataset/parameter dropdowns in `src/lexcube-client/src/client/index.html`<br>metadata from `/api/datasets` in `src/lexcube-client/src/client/networking.ts`<br>selection in `src/lexcube-client/src/client/interaction.ts`<br>**HOW TO USE**: open the dataset dropdown, pick a dataset, then choose a parameter click the Options (data-select) button to open the Cube/Parameter selects, then pick a dataset and parameter to trigger `selectCube`/`selectParameter` |
| Use custom datasets             | Visualize your own data cube.                                                             | **ACCESS**: pass 3D `numpy.ndarray` or `xarray.DataArray` into `Cube3DWidget` in `lexcube/cube3d.py`<br>shape validation in `lexcube/lexcube_server/src/lexcube_widget.py`<br>served via widget tile server<br>**HOW TO USE**: create the widget with your data (e.g., `Cube3DWidget(my_array)`) input must be a 3D `numpy.ndarray` or `xarray.DataArray` (other types or non-3D raise errors) | **ACCESS**: configure tile server `lexcube/lexcube_server/config_example.json` + `lexcube/lexcube_server/src/tile_server.py`<br>supports local files and remote HTTP/S3 via `fsspec` in `tile_server.py` `open_dataset`<br>**HOW TO USE**: add your dataset to config, restart server, then select it in the UI set `datasetBaseDir` and add a dataset entry with `id`, `shortName`, and `datasetPath` (e.g., `.zarr`/`.nc` local path or http/s3 URL opened via `open_dataset`) |
| Multi-dataset server features   | Manage multiple datasets, parameters, and anomalies on the server.                        | | **ACCESS**: server config `lexcube/lexcube_server/config_example.json`<br>implementation in `lexcube/lexcube_server/src/tile_server.py` dataset config, parameter allow/ignore lists, and `calculateYearlyAnomalies` with `_lxc_anomaly` parameters<br>**HOW TO USE**: edit config to add datasets/parameters or enable anomalies, then restart the tile server add multiple entries under `datasets`, set `onlyParameters`/`ignoredParameters` to control parameter lists, and set `calculateYearlyAnomalies` to expose `_lxc_anomaly` virtual parameters |
| Deep-link selection state       | Share a URL that includes cube/parameter selection and ranges.                            | | **ACCESS**: URL query string using `!` and `/` separators<br>parsed and updated in `src/lexcube-client/src/client/interaction.ts` `parseUrlFragment`/`updateUrlFragment`<br>**HOW TO USE**: interact with the cube to update the URL, then copy the URL to share the state load the client with a query string like `?` + `!<cube>/<parameter>/<zmin>-<zmax>/<ymin>-<ymax>/<xmin>-<xmax>` to restore a saved selection |
| Scripted automation             | Drive the client via browser console functions (e.g., select cube/parameter, screenshot). | | **ACCESS**: `?scripted` flag<br>window hooks in `src/lexcube-client/src/client/client.ts` set `window.selectCube`, `window.selectParameter`, `window.downloadScreenshotFromConsole`, etc.<br>**HOW TO USE**: open the page with `?scripted` and call the window helpers from the browser console call `window.getAvailableCubes()`, `window.selectCube(id)`, `window.getAvailableParameters()`, `window.selectParameter(name)`, or `window.downloadScreenshotFromConsole()` |
| Advanced modes                  | Enable debug/expert/studio/noUi/orchestration/textureFiltering modes.                     | | **ACCESS**: URL flags in `src/lexcube-client/src/client/client.ts`: `debug`, `expert`, `studio`, `noUi`, `orchestrationMinion`, `orchestrationMaster`, `scriptedMultiView`, `textureFiltering`, `isometric`<br>**HOW TO USE**: append the desired flags to the URL (e.g., `?debug&expert`) use the standalone (non-widget) client URL since flags are detected via `document.URL.indexOf(...)` only when `widgetMode` is false |
| Isometric view                  | Render the cube with isometric projection.                                                | **ACCESS**: `isometric_mode` traitlet in `lexcube/cube3d.py`<br>applied in `src/widget.ts` and `src/lexcube-client/src/client/client.ts`<br>**HOW TO USE**: set `isometric_mode=True` when creating the widget or on the widget instance you can also toggle after creation via `widget.isometric_mode = True`, and the widget passes that traitlet into `CubeClientContext` on render | **ACCESS**: `?isometric` URL flag in `src/lexcube-client/src/client/client.ts`<br>applied in `CubeClientContext`<br>**HOW TO USE**: open the page with `?isometric` add `&isometric` to any existing query string since the client enables isometric mode when `document.URL` contains `isometric` |
| Display quality control         | Change render quality / resolution.                                                       | | **ACCESS**: quality select in `src/lexcube-client/src/client/index.html`<br>applied in `src/lexcube-client/src/client/interaction.ts`<br>**HOW TO USE**: pick a quality preset from the quality dropdown open the Options panel (expert mode) and choose a Display Quality preset to update displayQuality and refresh visibility/LODs |
| Dimension wrapping (overflow)   | Allow selection to wrap around a dimension (e.g., global longitude).                      | **ACCESS**: `xwrap/ywrap/zwrap` traitlets in `lexcube/cube3d.py`<br>applied via `src/widget.ts` -> `rendering.updateOverflowSettings`<br>**HOW TO USE**: set `xwrap`, `ywrap`, or `zwrap` on the `Cube3DWidget` (e.g., `widget.xwrap = True`) and then set `xlim/ylim/zlim` within the doubled range allowed for wrapped axes to wrap selection | **ACCESS**: dataset tags inferred in `src/lexcube-client/src/client/interaction.ts`<br>applied via `rendering.updateOverflowSettings`<br>no UI toggle<br>**HOW TO USE**: load a dataset tagged `OverflowX` (e.g., an ESDC dataset or a global lon/lat cube with value ranges > 350/170) so overflow wrapping is enabled automatically for the X dimension |
| Scale cube axes                 | Stretch or compress the cube dimensions for visual emphasis.                              | **ACCESS**: `cube_scale` traitlet in `lexcube/cube3d.py`<br>passed into `CubeClientContext` in `src/widget.ts` constructor<br>used in `src/lexcube-client/src/client/client.ts` and rendering pipeline<br>**HOW TO USE**: pass `cube_scale=[x_scale, y_scale, z_scale]` when creating `Cube3DWidget(...)` or set `widget.cube_scale = [...]` to scale axes | |
| Save/restore camera angle       | Reproduce exact camera position/rotation for consistent views.                            | **ACCESS**: `camera_angle` traitlet in `lexcube/cube3d.py`<br>sync in `src/widget.ts` `cameraAngleChanged` and `updateWidgetCameraAngle`<br>**HOW TO USE**: pass `camera_angle=[x,y,z,rx,ry,rz]` when constructing `Cube3DWidget` or assign `widget.camera_angle = [...]` to restore a view, and read `widget.camera_angle` (updated from the current camera) to save the current view | **LIMITED**: URL `camera=<face>` selects preset face in `src/lexcube-client/src/client/interaction.ts` `applyCameraPreset`<br>no full numeric camera API in UI<br>**HOW TO USE**: open the client with `?camera=Front` (or Back/Top/Bottom/Left/Right) to apply the matching `Single Face (...)` preset |
| Export selected data            | Retrieve the data sub-cube for the current selection.                                     | **ACCESS**: `Cube3DWidget.get_current_cube_selection()` in `lexcube/cube3d.py`<br>supports index-only or data slicing for numpy/xarray<br>**HOW TO USE**: call `get_current_cube_selection()` to return the selected sub-cube, or pass `return_index_only=True` to get the selection indices (and optionally `data_to_be_indexed` to slice a specific array) | |
| Download progress feedback      | Show tile download progress and status messages.                                          | **ACCESS**: progress updates sent from widget server<br>`request_progress` traitlet in `lexcube/cube3d.py` and UI update in `src/widget.ts` -> `updateRequestProgressFromWidget`<br>**HOW TO USE**: when you load data in the widget, watch the status message for “Accessing data (%)...” (and remaining-time hints) while tiles download | **ACCESS**: status/tiles progress in `src/lexcube-client/src/client/interaction.ts` using download counters from `tiledata.ts`<br>**HOW TO USE**: when tiles are downloading, watch the status message for “Downloading...” (and any failure notices) as progress updates |
| External slider panel           | Show separate ipywidgets sliders outside the canvas.                                      | **ACCESS**: `Cube3DWidget.show_sliders()` in `lexcube/cube3d.py`<br>implemented by `Sliders` class in same file<br>links to `xlim/ylim/zlim`<br>**HOW TO USE**: call `Cube3DWidget.show_sliders()` on a widget instance to display the linked sliders | |
| Interactive tutorial            | Show mouse/touch tutorial overlay.                                                        | | **ACCESS**: tutorial overlay in `src/lexcube-client/src/client/index.html`<br>touch/mouse detection logic in inline script<br>**HOW TO USE**: the tutorial overlay appears on load (hidden on localhost), shows mouse vs touch instructions based on touch detection, and can be dismissed by clicking the overlay or the “Close Tutorial” button |
| Unit conversion and formatting  | Display values in human-friendly units and precision.                                     | **ACCESS**: hover and legend formatting in `src/lexcube-client/src/client/interaction.ts`<br>unit conversion tied to parameter metadata<br>**HOW TO USE**: select a parameter, then hover a cube face to see converted values with units in the hover panel, and read the color-scale legend labels/units which apply metadata-based conversions (e.g., K→°C, J/m^2→MJ/m^2) and auto-set numeric precision from the data range | **ACCESS**: hover and legend formatting in `src/lexcube-client/src/client/interaction.ts`<br>unit conversion tied to parameter metadata<br>**HOW TO USE**: select a parameter, then hover a cube face to see converted values with units in the hover panel, and read the color-scale legend labels/units which apply metadata-based conversions (e.g., K→°C, J/m^2→MJ/m^2) and auto-set numeric precision from the data range |
